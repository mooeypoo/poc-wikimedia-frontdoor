/**
 * Codex checkbox `input-value` tokens for explorer REST endpoint opt-in filters.
 */

/** Opt-in checkbox value for beta REST endpoints. */
export const EXPLORER_OPT_IN_VALUE_BETA_ENDPOINTS = 'beta-endpoints'

/** Opt-in checkbox value for internal REST endpoints. */
export const EXPLORER_OPT_IN_VALUE_INTERNAL_ENDPOINTS = 'internal-endpoints'
