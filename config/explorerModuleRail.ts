/**
 * config/explorerModuleRail.ts — **Legacy** custom module rail layout constants.
 *
 * Unused in product UX while `EXPLORER_USE_INTERNAL_SCALAR_SIDEBAR` is true (PR #40).
 */

/**
 * Maximum endpoint rows shown in the inline collapsible module rail before the
 * endpoint scrollport scrolls internally (`useExplorerModuleRailInlineEndpointScrollCap`).
 */
export const EXPLORER_MODULE_RAIL_INLINE_MAX_VISIBLE_ENDPOINTS = 7
