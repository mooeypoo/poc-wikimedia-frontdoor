/**
 * Exploratory surface tokens for explorer project controls and module rail.
 *
 * CSS custom properties in `app/assets/css/page-grid.css`
 * (`--fd-explorer-controls-surface-*`) must stay in sync with these values.
 */
export const EXPLORER_CONTROLS_SURFACE_BACKGROUND_COLOR = '#f3f3f3'

/** Border radius (px) for explorer control surfaces — exploratory Codex value (base token is 2px). */
export const EXPLORER_CONTROLS_SURFACE_BORDER_RADIUS_PX = 4
